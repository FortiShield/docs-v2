# Code blocks

### Python

```python
msg = "Hello World"
```

### Go

```python
msg := "Hello World"
```

### Bash

```bash
npm i @egiftcard/react
```

### Diff

```diff
- npm i @egiftcard/react
+ yarn add @egiftcard/react
```

### JS

```python
const msg = "Hello World"
```

### JSX

```jsx
const Msg = ({ text }) => {
	return <div>{text}</div>;
};

<Msg text="hello world" />;
```

### TS

```ts
const msg: string = "Hello World";
```

### TSX

```jsx
const Msg: React.FC<{ text: string }> = ({text }) => {
	return <div>{text}</div>;
}

<Msg text="hello world" />
```
